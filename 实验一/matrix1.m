%矩阵的建立
m1=1:2:4;
m2=[1 2 3 6 7];
m3=linspace(1,23,19);

%矩阵的操作

% 1.索引取值
a=m2(1,2);
b=m2(2);
c=a==b;
%将矩阵对应的行号和列号转化为索引号
d=sub2ind(size(m2),1,2);
%将矩阵对应的索引号转化为行号和列号
e=ind2sub(size(m2),6);
%找出相同值对应的索引
f=find(m2==6);
% 2.重排矩阵
g=reshape(m2,5,1);
% 3.矩阵转置
h=m2';
% 4.矩阵拆分
i1=m2(1,:);
i2=m2(1,1:2);
i3=m2(1,[1 2]);
i4=m2(1,1:end-1);
% 5.矩阵扩展
j=repmat(m2,2,2);
% 6.矩阵压缩
z=[1 1 2 2 3 3 4 4];
z1=unique(z);
% 7.删除矩阵元素
m2(1)=[]